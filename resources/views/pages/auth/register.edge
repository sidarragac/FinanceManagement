@layout('layouts/main', { title: 'Crear Cuenta - Finance Management' })

  @slot('main')
    <div class="max-w-md mx-auto bg-white p-8 rounded-xl border border-gray-200 shadow-sm">
      <h2 class="text-2xl font-bold text-gray-900 text-center mb-2">Crea tu cuenta</h2>
      <p class="text-sm text-gray-500 text-center mb-6">Comienza a gestionar tu dinero en segundos</p>

      <form action="{{ route('auth.register.store') }}" method="POST" class="space-y-5">
        {{ csrfField() }}

        {{-- Nombre Completo --}}
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nombre Completo</label>
          <input
            type="text"
            id="name"
            name="name"
            value="{{ old('name') || '' }}"
            required
            class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition {{ flashMessages.has('errors.name') ? 'border-red-500' : 'border-gray-300' }}"
            placeholder="Juan Pérez"
          >
          @if(flashMessages.has('errors.name'))
            <p class="text-xs text-red-500 mt-1">{{ flashMessages.get('errors.name') }}</p>
          @endif
        </div>

        {{-- Email --}}
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Correo Electrónico</label>
          <input
            type="email"
            id="email"
            name="email"
            value="{{ old('email') || '' }}"
            required
            class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition {{ flashMessages.has('errors.email') ? 'border-red-500' : 'border-gray-300' }}"
            placeholder="tu@email.com"
          >
          @if(flashMessages.has('errors.email'))
            <p class="text-xs text-red-500 mt-1">{{ flashMessages.get('errors.email') }}</p>
          @endif
        </div>

        {{-- Contraseña --}}
        <div>
          <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Contraseña</label>
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition {{ flashMessages.has('errors.password') ? 'border-red-500' : 'border-gray-300' }}"
            placeholder="Mínimo 8 caracteres"
          >
          @if(flashMessages.has('errors.password'))
            <p class="text-xs text-red-500 mt-1">{{ flashMessages.get('errors.password') }}</p>
          @endif
        </div>

        <button
          type="submit"
          class="w-full bg-indigo-600 text-white font-medium py-2.5 rounded-lg hover:bg-indigo-700 transition duration-150 shadow-sm"
        >
          Crear Cuenta
        </button>
      </form>

      <p class="text-xs text-center text-gray-500 mt-6">
        ¿Ya tienes una cuenta?
        <a href="{{ route('auth.login.index') }}" class="text-indigo-600 font-semibold hover:underline">Inicia sesión</a>
      </p>
    </div>
  @end
@end
