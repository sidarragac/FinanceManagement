@layout('layouts/main', { title: viewData.title })

  @slot('main')
    <div class="max-w-lg mx-auto">
      <div class="mb-6 flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold text-gray-900">{{ viewData.title }}</h1>
          <p class="text-sm text-gray-500">Actualiza los datos de tu categoría</p>
        </div>
        <a href="{{ route('categories.index') }}" class="text-sm font-medium text-gray-600 hover:text-gray-900">
          &larr; Volver
        </a>
      </div>

      <div class="bg-white p-8 rounded-xl border border-gray-200 shadow-sm">
        <form action="{{ route('categories.update', { id: viewData.category.id }) }}?_method=PATCH" method="POST" class="space-y-5">
          {{ csrfField() }}

          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nombre de la Categoría</label>
            <input
              type="text"
              id="name"
              name="name"
              value="{{ old('name') || viewData.category.name }}"
              required
              class="w-full px-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition {{ flashMessages.has('errors.name') ? 'border-red-500' : 'border-gray-300' }}"
            >
            @if(flashMessages.has('errors.name'))
              <p class="text-xs text-red-500 mt-1">{{ flashMessages.get('errors.name') }}</p>
            @endif
          </div>

          <div>
            <label for="color" class="block text-sm font-medium text-gray-700 mb-1">Color Identificador</label>
            <div class="flex items-center gap-3">
              <input
                type="color"
                id="color"
                name="color"
                value="{{ old('color') || viewData.category.color || '#4F46E5' }}"
                class="h-10 w-14 p-1 rounded border border-gray-300 cursor-pointer"
              >
              <span class="text-xs text-gray-500">Selecciona un color para diferenciar la categoría</span>
            </div>
          </div>

          <div class="flex items-center justify-end gap-3 pt-4 border-t border-gray-100">
            <a href="{{ route('categories.index') }}" class="px-4 py-2 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-100 transition">
              Cancelar
            </a>
            <button
              type="submit"
              class="bg-indigo-600 text-white font-medium px-5 py-2 rounded-lg hover:bg-indigo-700 transition duration-150 shadow-sm text-sm"
            >
              Actualizar Categoría
            </button>
          </div>
        </form>
      </div>
    </div>
  @end
@end
